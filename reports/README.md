# Reports Directory

Generated assessment reports will be saved here.

This directory is excluded from version control via `.gitignore`.

Reports include:
- PDF summaries (`summary_*.pdf`)
- CSV exports (`storage_accounts_*.csv`, `findings_*.csv`)
- JSON data (`storage_assessment_*.json`)
- Markdown summaries (`summary_*.md`)
